@model IEnumerable<SionFashioWebApplicationMVC.Models.sub_categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre_sub_categoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_categoriaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_sub_categoria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_categoriaNavigation.id_categoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_sub_categoria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_sub_categoria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_sub_categoria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
